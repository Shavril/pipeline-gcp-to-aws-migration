# Screenshots referenced by the top-level README

Add these here once available — the main README links to them as placeholders:

- `dashboard.png` — the original (GCP/BigQuery) Data Studio dashboard
- `dashboard-aws.png` — the Data Studio dashboard connected to Redshift (the AWS side)
- `dagster-asset-lineage.png` — the Dagster UI's asset lineage graph, from the raw RRC files
  through DuckDB, Parquet, S3, Redshift, the star schema, and the Redshift views
